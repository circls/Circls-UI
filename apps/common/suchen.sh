
# find ./ -name "index.html*" -type f -print0 | xargs -0 rm

# find ./ -name "*.*" -type f -print0 | xargs -0 grep 'Cluster Übersicht'

# änderung cb_rates  lager:debug("trying to save ~b rates (took ~b ms to process)", [Count, wh_util:elapsed_ms(Now)]),lists:foreach(fun(Rate) -> crossbar_doc:ensure_saved(cb_context:set_doc(Context, Rate), [{'publish_doc', 'false'}]) end, Rates),
# änderung /usr/local/lib/erlang/erts-5.9.3/include in  MAKEFILE  application/registrar


find ./ -name "*.js" -type f -print0 | xargs -0 grep -i '../../'

#'handle_reload_gateways'
